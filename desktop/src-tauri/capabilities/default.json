{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core window capability for the local-only game wrapper.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

